import React from "react";
import {
  HeroContainer,
  InfoContainer,
  ImageContainer,
  CountersContainer,
  SliderContainer,
} from "../components";

const Homepage = () => {
  return (
    <main>
      <HeroContainer />
      <InfoContainer />
      <ImageContainer />
      <CountersContainer />
      <SliderContainer />
    </main>
  );
};

export default Homepage;
